<%= form_with(model: @dish_pairing, local: true) do |f| %>

  <%= f.hidden_field :dish_id, value: @dish.id %>

  <div class="form-group">
    <%= f.label :wine_id %>

    <%= f.collection_select :wine_id, Wine.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>

    <%= f.text_field :description, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :share_link %>

    <%= f.text_field :share_link, class: "form-control" %>
  </div>

  <%= f.submit "Create dish pairing", class: "btn btn-block btn-outline-secondary" %>
<% end %>
